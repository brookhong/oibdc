CREATE TABLE note (id BIGINT AUTO_INCREMENT, title VARCHAR(255), content TEXT NOT NULL, tag VARCHAR(255) NOT NULL, created_at DATETIME NOT NULL, updated_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
